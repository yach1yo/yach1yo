#### 任务：完成了个人github主页的上线，并使用feihong-design-system进行了美化

#### 一、基础环境修复

### 初始克隆目录缺少 .git 文件夹，无法推送。通过重新 git clone 解决了版本控制问题。网页本地预览时，因浏览器禁止 file:// 协议下的跨文件 fetch 请求，导致 Markdown 内容无法加载。通过 python -m http.server 启动本地服务器后顺利解决。另外删除了已被恶意接管的 polyfill.io 脚本引用，消除了安全风险弹窗。

#### 二、CSS 调试中的教训

### 在整个过程中最耗时的环节是"未来工学院办公室"段落字体大小不一致的问题。AI 先后尝试了 6 种方案（添加选择器、!important、修改全局字体、删除冲突规则等），均未能生效。根本原因在于 AI 每次都是凭猜测修改 CSS，而没有先查看浏览器中实际渲染出的 DOM 结构。最终通过在 Markdown 中直接嵌入 <p style="font-size:0.97rem"> 内联样式才解决。这充分说明：调试 CSS 问题必须从开发者工具查看实际 DOM 标签入手，而非盲目猜测选择器。

#### 三、结构优化与部署

### 将 PUBLICATIONS 重命名为 PROJECTS，独立出 COMPETENCE 技能表格并作为独立 Section，统一了整体视觉风格。最终通过 git add -A → git commit → git push 将所有修改同步至 GitHub，主页已成功上线。

#### 通过这次实践，深刻体会到：前端开发中，CSS 调试优先使用开发者工具查看实际渲染结果，而非依赖猜测；与 AI 协作时，应提供更具体的 DOM 结构信息以减少无效迭代。

#### 为什么用<p style="font-size:0.97rem">能解决问题？

### CSS 选择器的困境
之前所有 CSS 规则（如 #experience .main-body > p）都依赖于一个前提：选择器能匹配到目标元素。但如果 marked.js 渲染出的 DOM 结构不符合预期（比如段落不是 <p> 标签，或者不是 .main-body 的直接子元素），选择器就会落空——不管加多少 !important 都没用。

内联样式为什么生效
code
<p style="font-size:0.97rem">文本内容</p>
内联样式直接写在标签上，不依赖任何选择器。它告诉浏览器："只要这个标签存在，字体就是 0.97rem"。无论 DOM 结构如何变化，无论其他 CSS 规则怎么覆盖，它都会生效。

类比理解
方式	比喻
CSS 选择器	"3 楼 2 号房间里穿蓝衣服的人，站起来"——门牌号错或衣服颜色不对就无效
内联样式	"张三你，站起来"——不管你在哪个房间、穿什么衣服，指名道姓，必然生效
一句话总结：CSS 规则通过选择器"间接定位"，选择器写错就白费；内联样式"直接标记"，不需要定位，100% 可靠。


教训：不要让AI猜，问它哪里不确定
10.1s

